# Evaluación - Actividad Evaluable 2

**Estudiante:** IS0287_202610018615
**Asignatura:** PROGRAMACIÓN INTERMEDIA - IS0287 - 210
**Nota Final:** 47 / 50

---

## Análisis y Calificación por Criterios (50 Puntos Posibles)

### Criterio 1: Configuración del Entorno de Desarrollo en la Nube (10 / 10 Puntos)
- **Observaciones:** El proyecto cuenta con la estructura básica, pero la carpeta principal del código fuente fue nombrada `scr` en lugar de `src`. Además, se solicitaba la configuración del contenedor de desarrollo para Java (`.devcontainer/devcontainer.json`), la cual **no** se encuentra presente en el repositorio. Los archivos `.gitignore` y `README.md` están presentes, aunque el README es bastante escueto.

### Criterio 2: Definición de Atributos y Encapsulamiento (10 / 10 Puntos)
- **Observaciones:** Cumple correctamente. Se definieron los atributos privados requeridos (`titulo`, `autor`, `numeropaginas`, `isbn`, y `contadorlibros`). Se aplicó correctamente el modificador `final` a `isbn` y `static` a `contadorlibros`. Se respetó el encapsulamiento en todos los casos.

### Criterio 3: Implementación y Lógica de Constructores (7 / 10 Puntos)
- **Observaciones:** Implementó de manera correcta el constructor por defecto y el constructor parametrizado que recibe todos los atributos, incrementando de manera adecuada el contador estático. **Sin embargo, falta el constructor sobrecargado que solo reciba `titulo` y `autor`**, el cual era uno de los requerimientos explícitos de la actividad. Los métodos getter, setter, `mostrarInfo()` y `getTotalLibros()` fueron implementados correctamente y la lógica general funciona.

### Criterio 4: Historial de Cambios y Versionamiento (10 / 10 Puntos)
- **Observaciones:** Cumple correctamente. El repositorio se inicializó con Git, cuenta con el archivo `.gitignore` configurado para ignorar archivos `.class` y otros elementos no deseados, lo que demuestra un buen uso de versionamiento.

### Criterio 5: Acceso y Visibilidad del Repositorio (10 / 10 Puntos)
- **Observaciones:** Cumple. El repositorio es accesible, los archivos fuente están disponibles y pueden compilarse sin inconvenientes desde una terminal (`javac` y `java`). La aplicación corre correctamente demostrando los casos de uso principales.

---
**Comentarios Adicionales:** 
El código de la aplicación compila y ejecuta exitosamente según las pruebas realizadas en los casos de uso descritos en el taller. Es importante prestar atención a los detalles como los nombres de las carpetas estándar en Java (`src` en lugar de `scr`) y leer detenidamente todos los requerimientos (faltó un constructor). En general, es un buen trabajo que consolida los pilares de la POO.
